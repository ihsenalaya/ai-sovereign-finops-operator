# AKS Confidential Terraform

Ce dossier est réservé au dry-run et à la préparation article 1.

Contraintes:
- `AZURE_APPLY_APPROVED=false` par défaut
- registry `ghcr.io`
- pas de dépendance à `ACR`
- pas de GPU
- pas de TDX réel
