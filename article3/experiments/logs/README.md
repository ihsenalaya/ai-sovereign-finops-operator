# Run logs

Each run stores a manifest, hashes, exact observation accounting, redacted logs, diagnostics, and failure/exclusion reasons. Logs are evidence only when their code/config/data hashes match the registry.
