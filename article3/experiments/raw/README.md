# Frozen-run raw data

This directory is intentionally clean after the 2026-07-11 quarantine. Only append immutable outputs created under the current frozen protocol. Archived Codex-era JSON is forbidden as an analysis input.
