---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: aiadmissionapprovaldecisions.aiops.imperium.io
spec:
  group: aiops.imperium.io
  names:
    kind: AIAdmissionApprovalDecision
    listKind: AIAdmissionApprovalDecisionList
    plural: aiadmissionapprovaldecisions
    shortNames:
    - aiaad
    singular: aiadmissionapprovaldecision
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.decision
      name: Decision
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIAdmissionApprovalDecision is created only by a separately authorized human
          reviewer. The admission service has no permission on this resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              decision:
                description: AIAdmissionApprovalDecisionValue is the immutable human
                  decision.
                enum:
                - Approved
                - Rejected
                type: string
              proposalName:
                type: string
              proposalUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              reason:
                type: string
              requestDigest:
                type: string
            required:
            - decision
            - proposalName
            - proposalUID
            - requestDigest
            type: object
        type: object
        x-kubernetes-validations:
        - message: decision name must equal proposalName
          rule: self.metadata.name == self.spec.proposalName
        - message: admission approval decision spec is immutable
          rule: self.spec == oldSelf.spec
    served: true
    storage: true
    subresources: {}
