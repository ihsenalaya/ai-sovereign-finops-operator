% =====================================================================
%  Economic-Aware and Sovereignty-Constrained Routing for Enterprise
%  LLM Gateways  --  full article (Draft v0.6)
%
%  Compiles on Overleaf with the default pdfLaTeX + BibTeX pipeline,
%  and locally with `tectonic main.tex`. All figures are in ./figures/
%  and the bibliography is ./references.bib  (no external paths).
% =====================================================================
\documentclass[11pt,a4paper]{article}

% ---- encoding & fonts (portable on pdfLaTeX) ------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{textcomp}

% ---- math, tables, figures, layout ---------------------------------
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{xcolor}
\usepackage{pifont}            % \ding{51} check, \ding{55} cross
\usepackage[margin=1in]{geometry}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage[numbers,sort&compress]{natbib}

% ---- convenience macros --------------------------------------------
\newcommand{\cmark}{\textcolor{green!55!black}{\ding{51}}}
\newcommand{\xmark}{\textcolor{red!70!black}{\ding{55}}}
\newcommand{\Ours}{\textbf{Ours}}
\graphicspath{{figures/}}
\captionsetup{font=small,labelfont=bf}
\setlength{\parskip}{0.35em}
\renewcommand{\arraystretch}{1.15}

% =====================================================================
\title{\bfseries Economic-Aware and Sovereignty-Constrained Routing\\[2pt]
       for Enterprise LLM Gateways}
\author{(authors omitted for review)}
\date{Draft v0.7 --- conservative evidence labels, sensitivity, bounded live kind verification, and expanded bibliography}

\begin{document}
\maketitle

% ---------------------------------------------------------------------
\begin{abstract}
\noindent
Enterprises are adopting large language models (LLMs) faster than they can govern
them: they cannot say which model truly costs the most, which team consumes what,
when to route to a cheaper model, what a data-sovereignty constraint costs, or
when self-hosting beats a managed API. We present an \emph{economic-aware,
sovereignty-constrained, budget-aware routing control plane} for enterprise LLM
gateways. We formalize model selection as a constrained scoring problem that
rejects any model violating declarative sovereignty rules and, among the rest,
minimizes a weighted combination of cost, expected quality loss, latency, and
budget pressure. We realize the approach as a Kubernetes operator whose FinOps
and sovereignty engines compute auditable decisions and telemetry, and evaluate
it with explicitly separated evidence classes. In the cached experimental
harness, real LLM calls across OpenAI (US, managed) and Mistral (EU, Azure AI
Foundry DataZoneStandard) are replayed deterministically from committed caches.
Relative to an always-premium policy, the governance policy reduces cached
measured cost by \textbf{70.8\%} while keeping LLM-as-judge quality comparable to
premium; a \textbf{30-repetition} uncached statistical run (temperature 0.7)
confirms the cost reduction is highly significant
($p\approx3\times10^{-11}$, Cohen $d\approx-43$) at a significant but bounded
latency increase. In simulated policy scenarios it produces \textbf{zero
sovereignty violations} (vs.\ 40 for a sovereignty-blind baseline) and sustains
100\% request availability with 0\% budget overrun under a tight budget where
hard-blocking drops to 60\%. A bounded live kind verification then shows the
operator scraping real Envoy AI Gateway token telemetry from four apps
(41 input and 897 output tokens, 7655\textmu EUR reported in status) and
detecting real shadow-AI egress through Tetragon. On quality we are deliberately
careful: on
\textbf{500 objective public-benchmark items} (GSM8K + MMLU) our policy
\emph{trades accuracy for cost} (47.2\% vs.\ 65.4\% premium, $-94\%$ cost), and
we show the LLM judge \emph{over-rates incorrect answers} (81.1\% of wrong
answers rated ``acceptable''). We frame the contribution as a \textbf{governance
layer} with a favourable cost/quality trade-off, not a quality-free lunch, and
release all code, datasets, cached responses, and analysis as a reproducible
framework.
\end{abstract}

% =====================================================================
\section{Introduction}
LLMs are now embedded in HR assistants, document RAG, developer tooling, and
analytical agents, following the rapid scaling of foundation models and both
closed and open deployments~\cite{brown2020language,bommasani2021foundation,
openai2023gpt4,touvron2023llama2}. Each call has a price that depends on the
model, the provider, and the token volume; each call may also send data across
jurisdictions. Three problems recur in regulated enterprises:
\begin{enumerate}
  \item \textbf{Cost opacity.} Teams cannot attribute spend per application/team
        or decide when a cheaper model would suffice.
  \item \textbf{Sovereignty/compliance.} Data may not leave a permitted zone, and
        sensitive prompts may not go to external providers; today this is enforced
        ad hoc, if at all.
  \item \textbf{Managed vs.\ self-hosted arbitrage.} It is unclear at what volume
        self-hosting on GPU becomes cheaper than a managed API.
\end{enumerate}

Existing gateways and FinOps tools mostly \emph{measure and report}, as seen in
cloud FinOps practice and Kubernetes cost-allocation systems
\cite{finopsframework,opencost}. Our thesis is that a gateway-adjacent control
plane can compute and expose auditable routing
decisions: select a model that meets quality and hard sovereignty constraints,
degrade gracefully under budget pressure instead of blocking, attribute spend by
Kubernetes workload, and surface the self-hosting break-even from observed usage.
Full production-grade data-path enforcement, load testing, and failover remain
outside the claims of this paper.

\paragraph{Contributions.}
\begin{itemize}
  \item A constrained scoring formulation for LLM routing unifying cost, quality,
        latency, sovereignty, and budget (\S\ref{sec:formulation}).
  \item A Kubernetes/Envoy control-plane realization: the \emph{AI Sovereign
        FinOps Operator}, exposing Cost, Budget, Sovereignty, Break-even, and
        Report engines as CRDs (\S\ref{sec:design}).
  \item A reproducible evaluation methodology and testbed driving the operator's
        actual engines with real LLM telemetry (\S\ref{sec:method}).
  \item A bounded live kind verification that exercises Envoy AI Gateway
        telemetry, Kubernetes attribution, Azure Foundry Cohere/Mistral calls,
        and Tetragon shadow-AI detection without manual seeding.
  \item Empirical evidence across six research questions
        (\S\ref{sec:results}), plus an objective public-benchmark study and an
        LLM-judge validity analysis.
\end{itemize}

% =====================================================================
\section{Background and Related Work}
\label{sec:related}

\paragraph{Model routing and cascades.}
A growing line of work trades quality for cost by selecting among models per
request. FrugalGPT~\cite{chen2023frugalgpt} cascades from cheap to expensive
models using a learned scorer; Hybrid LLM~\cite{ding2024hybridllm} routes by
predicted query difficulty with a tunable quality target;
RouteLLM~\cite{ong2024routellm} learns routers from preference data;
AutoMix~\cite{aggarwal2024automix} uses few-shot self-verification and a POMDP
router. Recent work extends this space with test-time compute selection,
preference-conditioned bandits, and dynamic mixed-model routing
\cite{ding2025bestroute,li2025llmbandit,wang2025mixllm}. These works optimize
a \emph{cost--quality} trade-off, largely via per-request
difficulty/confidence prediction for a single tenant. Our work is
\textbf{orthogonal and complementary}: they decide \emph{which model is good
enough}; we add the \textbf{governance layer} around that decision --- hard
data-sovereignty constraints, per-team/namespace budgets, budget-aware graceful
degradation, attribution, and hosting economics --- at the gateway/control-plane
level. Our routing score is intentionally simple (priors, not a learned
difficulty predictor); combining a learned difficulty router with our
sovereignty/budget constraints is explicit future work (\S\ref{sec:conclusion}).

\paragraph{LLM serving systems.}
Orca~\cite{yu2022orca}, vLLM/PagedAttention~\cite{kwon2023pagedattention},
FlexGen~\cite{sheng2023flexgen}, SGLang~\cite{zheng2024sglang},
FastServe~\cite{wu2023fastserve}, SpecInfer~\cite{miao2024specinfer},
Sarathi-Serve~\cite{agrawal2024sarathi}, DistServe~\cite{zhong2024distserve},
fair LLM schedulers~\cite{sheng2024fairness}, and
Llumnix~\cite{sun2024llumnix} optimize \emph{throughput/latency} or scheduling
of a single self-hosted serving layer; surveys
\cite{zhou2024surveyinference,zhen2025tamingtitans} organize this space. They are
complementary: they define the \emph{cost} of the self-hosted option whose
break-even our system predicts (RQ6), but do not address cross-provider routing,
attribution, or sovereignty.

\paragraph{LLM gateways.}
Envoy AI Gateway~\cite{envoyaigateway} and LiteLLM~\cite{litellm} provide a
unified data path to multiple providers; Envoy Gateway provides the underlying
gateway substrate~\cite{envoygateway}. These systems expose usage telemetry, but
leave \emph{policy} (which model, under which constraints, within which budget)
to the operator. We contribute that policy layer as a Kubernetes control plane,
using the operator/CRD pattern~\cite{kubernetesoperators,kubernetescrd}, and
validate it.

\paragraph{Evaluation and regulation.}
We follow the LLM-as-a-judge methodology of Zheng et al.~\cite{zheng2023judge},
alongside broader LLM evaluation and benchmark work
\cite{liu2023geval,liang2022helm,srivastava2022bigbench}. We adopt both
absolute and pairwise protocols while acknowledging documented judge biases
(\S\ref{sec:threats}). Data-residency and sensitive-data handling are driven by
the GDPR~\cite{gdpr2016}, the EU AI Act~\cite{euaiact2024}, AI governance
frameworks~\cite{nistairmf2023,nistgenai2024,iso42001}, and LLM security risk
taxonomies~\cite{owaspllm2025}; our system targets \emph{audit preparation}, not
legal attestation.

% ---- Table 1: feature matrix ---------------------------------------
\begin{table}[H]
\centering\small
\caption{Positioning feature matrix. Prior routing work optimizes the
cost--quality trade-off; we add the governance dimensions at the control-plane
level.}
\label{tab:matrix}
\begin{tabular}{lccccc}
\toprule
Capability & FrugalGPT & Hybrid LLM & RouteLLM & AutoMix & \Ours \\
\midrule
Cost-aware routing            & \cmark & \cmark & \cmark & \cmark & \cmark \\
Quality-aware routing         & \cmark & \cmark & \cmark & \cmark & \cmark \\
Learned difficulty predictor  & \cmark & \cmark & \cmark & \cmark & \xmark\,(priors) \\
\textbf{Hard sovereignty}     & \xmark & \xmark & \xmark & \xmark & \cmark \\
\textbf{Per-tenant budgets}   & \xmark & \xmark & \xmark & \xmark & \cmark \\
\textbf{Graceful degradation} & \xmark & \xmark & \xmark & \xmark & \cmark \\
\textbf{Cost attribution}     & \xmark & \xmark & \xmark & \xmark & \cmark \\
\textbf{Break-even (mgd/self)}& \xmark & \xmark & \xmark & \xmark & \cmark \\
Gateway/K8s artifact          & \xmark & \xmark & \xmark & \xmark & \cmark \\
\bottomrule
\end{tabular}
\end{table}

We do not claim to beat learned routers on the cost--quality frontier; our
governance layer is \emph{orthogonal} and could wrap any of them.

% ---- Table 2: side-by-side -----------------------------------------
\begin{table}[H]
\centering\small
\caption{Side-by-side vs.\ representative systems --- three learned routers and a
production gateway.}
\label{tab:compare}
\begin{tabular}{lccccc}
\toprule
Dimension & RouteLLM & FrugalGPT & AutoMix & LiteLLM & \Ours \\
\midrule
Type                       & router  & cascade & router  & gateway   & \textbf{ctrl plane} \\
Multi-provider             & part.   & \cmark  & \cmark  & \cmark    & \cmark \\
\textbf{Hard sovereignty}  & \xmark  & \xmark  & \xmark  & \xmark    & \cmark \\
\textbf{Budget + degrad.}  & \xmark  & \xmark  & \xmark  & basic     & \cmark \\
\textbf{Cost attribution}  & \xmark  & \xmark  & \xmark  & per-key   & \cmark \\
\textbf{Break-even}        & \xmark  & \xmark  & \xmark  & \xmark    & \cmark \\
K8s/Envoy artifact         & \xmark  & \xmark  & \xmark  & proxy     & \cmark \\
\bottomrule
\end{tabular}
\end{table}

LiteLLM is the closest \emph{engineering} peer: it is a production-oriented
gateway with spend tracking, budgets, keys/teams, retries, and fallbacks. We do
not position our operator as a replacement for such a gateway. The difference is
the Kubernetes-native governance surface around the gateway: CRDs, reconciliation
loops, namespace/application attribution, hard sovereignty-first filtering,
auditable decision traces, and modeled managed-vs-self-host economics. Learned
routers remain stronger on request-difficulty prediction; our contribution is the
governance layer designed to wrap such routers rather than replace them.

% =====================================================================
\section{Problem Formulation and Algorithm}
\label{sec:formulation}
A request $r$ is a tuple
$(team, namespace, application, sensitivity, \hat{t}_{in}, \hat{t}_{out})$.
The catalog $M$ contains candidate models; each model $m$ has provider metadata,
declared residency zone, managed/self-hosted flag, per-token price, quality prior
$q_m$, and latency prior $\lambda_m$. A sovereignty policy $P$ and budget state
$b=(used,total)$ are evaluated before scoring.

\paragraph{Hard sovereignty filter.} The feasible candidate set is
$C_P(r)=\{m\in M : compliant(m,r,P)\}$. A model is rejected if its zone is
forbidden, if an allow-list exists and its declared zone is outside that list
(``EU'' covers EU member-state zones), or if the request is sensitive and managed
external providers are disallowed. If $C_P(r)=\varnothing$, the request is
blocked or requires approval; this is a policy outcome, not a router failure.

\paragraph{Normalization.} For any scalar metric $x_m$ over candidates $C$, define
\[
N(x_m)=
\begin{cases}
0, & \min_C x = \max_C x,\\
(x_m-\min_C x)/(\max_C x-\min_C x), & \text{otherwise.}
\end{cases}
\]
Cost $C_m$ is the expected request price from $(\hat{t}_{in},\hat{t}_{out})$.
Quality loss is $\ell_q(m)=\max(0,q_{best}-q_m)$ before normalization. Latency
penalty is derived from observed p95/p99 when available, otherwise from catalog
priors. Budget pressure is $\rho=\min(1,used/total)$, with $\rho=0$ when no
positive budget is configured.

\paragraph{Soft objective.} Among feasible candidates, the router selects
\begin{equation}
\arg\min_{m\in C_P(r)}\;
  w_C\,N(C_m)(1+w_B\rho)
  + w_Q\,N(\ell_q(m))
  + w_L\,N(\lambda_m)
  + w_S\,\sigma(m),
\end{equation}
where $\sigma(m)\in[0,1]$ is residual policy risk after hard filtering. The
committed B6 implementation instantiates this heuristic with premium-normalized
cost, max-normalized latency, quality loss vs.\ premium, and defaults
$w_C{=}1.0,\ w_Q{=}1.5,\ w_L{=}0.3,\ w_S{=}0.5,\ w_B{=}1.0$ in
\path{experimentation/internal/router}. These are policy weights, not learned
parameters.

\paragraph{Budget phases and weights.} The operator exposes budget phases from
CRDs. The experimental B6 router keeps the base weights fixed and increases the
effective cost weight continuously:
$w_C^{eff}=w_C(1+w_B\rho)$, capped at $\rho=1$. With the committed defaults
($w_C{=}1,w_B{=}1$), this yields $w_C^{eff}\in[1.0,1.7)$ in NORMAL,
$[1.7,1.9)$ in PRESSURE, $[1.9,2.0)$ in CRITICAL, and $2.0$ in EXHAUSTED; in
EXHAUSTED the quality gate is relaxed to preserve availability when a compliant
candidate exists. Operational deployments may bind phases to discrete weight
presets, but the evaluated artifact uses the continuous form above:
\begin{table}[H]\centering\small
\caption{Budget phase to routing behaviour. Thresholds and effective cost weights
are CRD/code policy values in the current artifact, not hidden experimental
constants.}
\label{tab:budgetstate}
\begin{tabular}{p{2.5cm}p{3.0cm}p{4.3cm}p{3.3cm}}
\toprule
Phase & Trigger & Routing behaviour & Risk \\
\midrule
NORMAL / WithinBudget & $usage<70\%$ & $w_C^{eff}\in[1.0,1.7)$; require $q_{\min}$. & May leave savings unused. \\
PRESSURE / Warning & $70\%\le usage<90\%$ & $w_C^{eff}\in[1.7,1.9)$; alert. & Quality may drift if cost pressure is too aggressive. \\
CRITICAL & $90\%\le usage<100\%$ & $w_C^{eff}\in[1.9,2.0)$; prefer cheaper compliant fallbacks when guardrails allow. & Fallback can increase latency or lower quality. \\
EXHAUSTED / Exceeded & $usage\ge100\%$ & $w_C^{eff}=2.0$; hard-block policy may stop traffic, while B6 relaxes $q_{\min}$ and degrades if possible. & Availability vs.\ quality trade-off becomes explicit. \\
RECOVERY & usage drops below threshold & Current implementation recovers immediately; hysteresis/cooldown is future work. & Without cooldown, phase can oscillate near thresholds. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Algorithm.}
\begin{enumerate}
  \item Build candidate set from the model catalog and workload metadata.
  \item Apply hard sovereignty filtering and record every rejection reason.
  \item If no compliant candidate remains, apply the configured block/approval policy.
  \item Compute expected cost, quality loss, latency penalty, and budget pressure.
  \item Normalize scoring terms and select the weight policy for the current budget phase.
  \item Score each candidate and select the lowest-score model.
  \item Emit a decision trace, rejected-candidate list, and attribution labels.
\end{enumerate}
This is an auditable heuristic. We make no global optimality claim, and learned
request-difficulty predictors can be inserted behind the hard policy filter.

% =====================================================================
\section{System Design}
\label{sec:design}
Requests flow client $\to$ \textbf{Envoy AI Gateway} $\to$ provider (managed API
or self-hosted). The \textbf{AI Sovereign FinOps Operator} is the control plane:
CRDs declare gateways, providers, models, budgets, and sovereignty policies; pure
engines compute cost (per model/team/namespace), budget phase, sovereignty
findings, and break-even; a report engine emits Markdown/JSON and Prometheus
\texttt{ai\_finops\_*} metrics. The design keeps the data path in Envoy and the
decision logic in the Kubernetes operator. In this paper the routing decision is
evaluated primarily at the control-plane level. The repository also contains a
bounded live kind verification in which Envoy AI Gateway emits real token
telemetry and the operator attributes it per namespace/application. This
validates the telemetry and attribution path, not production-grade load,
failover, or legal compliance. OpenTelemetry-style correlation of metrics, logs,
and traces motivates the evidence model, while Tetragon supplies eBPF-based
runtime observability for direct, non-gateway LLM egress
\cite{opentelemetry,tetragon}.

\paragraph{Budget engine.} The budget engine is pure control-plane logic: it
collects usage samples, computes cost through the catalog price book, projects
monthly spend when the policy window is shorter than a month, and writes phase,
usage percentage, observed spend, projected spend, and advisory actions to
\texttt{AIBudgetPolicy.status}. The default manifests use warning/critical/hard
thresholds of 70/90/100 percent and actions
\texttt{alert}/\texttt{blockOrRequireApproval}. Gateway fallback actuation is only
attempted when explicitly configured with \texttt{enforcementMode=enforce},
\texttt{fallbackModelRef}, and guardrails; the live kind run used
\texttt{reportOnly}, so no hidden reroute was performed.

\paragraph{Sovereignty engine.} The sovereignty engine enforces declared
technical routing policies. Provider residency, managed/self-hosted status, and
sensitive-data eligibility come from catalog/configuration metadata, contracts,
or another trusted source supplied by the operator. The system does \textbf{not}
independently certify legal compliance or provider data-residency claims. Example
policies include EU-only, France-only, self-hosted-only, and
no-external-provider-for-sensitive-data. The output is an auditable finding or
decision trace, not a legal attestation.

\begin{table}[H]\centering\small
\caption{Example decision trace under an EU-only, sensitive-data policy. This is
ILLUSTRATIVE of the rule evaluation; source fields are declared metadata.}
\label{tab:decisiontrace}
\begin{tabular}{p{3.0cm}p{2.3cm}p{2.1cm}p{5.2cm}}
\toprule
Candidate & Declared zone & Decision & Reason \\
\midrule
\texttt{gpt-4o} & US & reject & Zone outside EU allow-list. \\
\texttt{gpt-4o-mini} & US & reject & Zone outside EU allow-list. \\
\texttt{mistral-large} & EU & accept & Declared EU residency and sensitive-data allowed. \\
\texttt{selfhosted-eu-llama} & FR & accept & FR is inside EU allow-list; self-hosted. \\
\bottomrule
\end{tabular}
\end{table}

% =====================================================================
\section{Methodology}
\label{sec:method}

\paragraph{Evidence labels.} To avoid mixing claims, each result is interpreted
under one of four labels. \textbf{MEASURED} means observed in a live execution or
fresh API run. \textbf{CACHED} means replayed from committed responses produced by
earlier real calls. \textbf{SIMULATED} means generated by deterministic policy or
workload simulation over declared catalogs. \textbf{MODELED} means computed from
an analytical model, such as GPU break-even economics. We do not present modeled
self-hosting economics as GPU/vLLM measurements.

\paragraph{Testbed.} A Go harness drives the operator's \emph{actual} engines and
issues \textbf{real} LLM calls across \textbf{two providers}: (1) \textbf{OpenAI
(US, managed)} --- gpt-4o (premium), gpt-4o-mini (medium), gpt-4.1-nano (cheap);
(2) \textbf{Mistral (EU)} --- \texttt{mistral-large} served via \textbf{Azure AI
Foundry} as \textbf{Mistral-Large-3} in \emph{DataZoneStandard} (EU data zone), a
real EU-hosted provider. A \emph{modeled} EU self-hosted model (cost computed,
response stubbed, rows tagged \emph{modeled}) is retained only for the RQ6
break-even prediction. The harness is provider-agnostic (per-provider
OpenAI-compatible clients), so adding providers is additive.

\paragraph{Workloads (W1--W4).} HR chatbot (short, sensitive), RAG docs (long
context, quality-sensitive), Dev assistant (high quality), Analytical agent (high
volume). Each declares team, namespace, sensitivity, monthly budget, premium
model, allowed models, and $q_{\min}$.

\paragraph{Execution parameters.} The deterministic harness uses temperature 0
and the committed response cache for repeatability. The statistical run bypasses
caches for 30 repetitions at temperature 0.7 and reports distribution-level
statistics. Public-benchmark exact-match evaluation uses 500 committed items
(GSM8K~\cite{cobbe2021gsm8k} +
MMLU~\cite{hendrycks2021mmlu}). The live kind verification was run on
2026-06-13. Pricing values are the catalog values committed with the experiment
and are not dynamically looked up during analysis; price drift is handled as
sensitivity, not hidden.
Per-call input/output tokens, model, provider, latency, cost, and blocked status
are recorded in \path{experimentation/results/calls.csv}. The harness preserves
failed runs in journals rather than silently retrying them into success; the live
kind apps use bounded attempt limits and expose HTTP status codes in the evidence
logs.

\paragraph{Baselines.} B1 premium-static, B2 round-robin, B3 least-cost, B4
static namespace policy, B5 budget hard-block, B6 ours; plus B7 a
literature-style difficulty router (Hybrid-LLM/RouteLLM proxy).

\paragraph{Metrics.} Cost (total, per request, per token, per team via the
operator cost engine, \% savings); quality (LLM-as-judge acceptability 1--5
$\to$ normalized 0--1, acceptable-rate $\geq3$, pairwise win-rate vs.\ premium);
latency (p50/p95/p99, mean, routing-decision $\mu$s); sovereignty (violations,
reroutes, blocked); budget (availability, overrun); break-even (managed vs.\
self-hosted monthly, payback). This multi-metric view follows the evaluation
lesson that accuracy alone is insufficient for deployed LLM systems
\cite{liang2022helm,srivastava2022bigbench}.

\paragraph{Reproducibility.} Temperature 0 for the deterministic pass; all
responses and judge scores cached so re-runs are identical and free; every test
journaled with status+duration (42 PASS / 0 FAIL). Latency 95\% CIs via
bootstrap. The protocol also supports the $\geq30$-repetition run reported in
\S\ref{sec:stats}.

\begin{table}[H]\centering\footnotesize
\caption{Reproducibility checklist and artifact mapping.}
\label{tab:artifactmap}
\begin{tabular}{p{3.0cm}p{1.4cm}p{5.0cm}p{4.0cm}}
\toprule
Artifact & Avail. & Path & Used for \\
\midrule
Workload prompts & yes & \path{experimentation/datasets/*.json} & RQ1--RQ6 harness inputs \\
Public benchmarks & yes & \path{experimentation/datasets-public/*.json} & RQ2b exact-match accuracy \\
Cached responses & yes & \path{experimentation/results/cache.json} & CACHED deterministic reruns \\
Harness CSVs & yes & \path{experimentation/results/*.csv} & Figures 2--8 and recap tables \\
Statistical run & yes & \path{experimentation/results-stats/} & N=30 significance results \\
Sensitivity CSVs & yes & \path{experimentation/results/*sensitivity.csv} & Table~\ref{tab:sensitivity} \\
Analysis scripts & yes & \path{experimentation/scripts/} & Figure/table regeneration \\
Live kind evidence & yes & \path{experimentation/results-live-kind-20260613-verify-v2/} & Table~\ref{tab:liveattr} and measured telemetry \\
Negative live run & yes & \path{experimentation/results-live-kind-20260613-verify/} & Preserved partial/negative evidence \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Bounded live kind verification.} In addition to the harness, we ran a
fresh local kind verification on 2026-06-13 using
\path{make -C automatisation real-demo-verify}. The run rebuilt the operator
image from the local source, deployed Envoy Gateway v1.5.0 and Envoy AI Gateway
v0.7.0, deployed four bounded apps, collected logs/CRDs/metrics/Tetragon
evidence, scaled the apps down, and deleted the kind cluster. Azure was used only
for the model endpoints; no AKS cluster or GPU/vLLM workload was created. The
evidence bundle is \path{experimentation/results-live-kind-20260613-verify-v2}.

% =====================================================================
\section{Results}
\label{sec:results}

\subsection{Live kind verification --- telemetry, attribution, and cleanup
(\textbf{MEASURED})}
The bounded live verification completed successfully on a fresh kind cluster and
then deleted the cluster. All four verification apps reached one real successful
gateway call and then idled: \texttt{rh/chatbot-rh},
\texttt{finance/risk-assistant}, and \texttt{legal/contract-review} called Azure
Foundry Cohere; \texttt{marketing/content-writer} called Azure Foundry Mistral.
Envoy AI Gateway exported per-namespace/application token telemetry:
10/219 input/output tokens for \texttt{rh/chatbot-rh}, 7/319 for
\texttt{finance/risk-assistant}, 10/299 for \texttt{legal/contract-review}, and
14/60 for \texttt{marketing/content-writer}. The operator reported
\texttt{totalInputTokens=41}, \texttt{totalOutputTokens=897}, and
\texttt{totalCostEUR=7655u} in the \texttt{AIFinOpsReport} status after the
sub-cent precision fix. Tetragon exported real process events for
\texttt{finance/rogue-script} connecting directly to \texttt{api.openai.com};
the resulting \texttt{shadow-egress} ConfigMap contained four observed
connections and the sovereignty policy reported seven findings.

This is a bounded integration verification, not a throughput benchmark. Its
purpose is to validate that the Kubernetes operator can consume real Envoy AI
Gateway token telemetry and real Tetragon shadow-AI evidence without manual
seeding, and that the automation stops apps and deletes the cluster after
collecting evidence.

\begin{table}[H]\centering\small
\caption{MEASURED live kind attribution from Envoy AI Gateway metrics
(2026-06-13). Costs use the declared catalog prices in the evidence bundle.}
\label{tab:liveattr}
\begin{tabular}{llrrr}
\toprule
Namespace / app & Model & Input tok. & Output tok. & Cost EUR \\
\midrule
\texttt{finance/risk-assistant} & \texttt{cohere-command-a-latest} & 7 & 319 & 0.002774 \\
\texttt{legal/contract-review} & \texttt{cohere-command-a-latest} & 10 & 299 & 0.002608 \\
\texttt{marketing/content-writer} & \texttt{mistral-large-latest} & 14 & 60 & 0.000357 \\
\texttt{rh/chatbot-rh} & \texttt{cohere-command-a-latest} & 10 & 219 & 0.001916 \\
\midrule
\textbf{Total} & & 41 & 897 & 0.007655 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{RQ1 --- Cost}
In the cached harness (\textbf{CACHED}), relative to premium-static,
\textbf{B6-ours cuts total cost by 70.8\%}
(0.01135 vs.\ 0.03893 EUR over the 40-prompt matrix; cost/request 0.000284 vs.\
0.000973). With the second provider available, Ours routes much traffic to the
cheaper Mistral EU and OpenAI mid/cheap tiers. A \emph{literature-style
difficulty router} (B7) saves slightly \emph{more} (73.1\%) but at lower quality
(0.866) and with no sovereignty/budget/attribution governance. Na\"ive least-cost
(B3) saves 98.2\% at the largest quality cost; round-robin 62.3\%; static policy
46.0\% (Figure~\ref{fig:cost}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig2_cost_by_strategy.png}
\caption{CACHED: total cost by routing strategy in the harness
(source: results/rq1\_cost.csv); lower is better, with catalog prices.}\label{fig:cost}
\end{figure}

\subsection{RQ2 --- Quality (LLM-as-judge)}
Within this two-provider, single-pass scope, B6-ours's mean normalized quality is
\textbf{comparable to --- marginally above --- premium} (0.9125 vs.\ 0.900;
acceptable-rate 97.5\%), with a pairwise win-rate vs.\ premium of \textbf{50.0\%}
(statistical parity). The cross-provider mix helps: Mistral-Large-3 sometimes
matches or beats gpt-4o on the judge. Least-cost drops to 0.858, static-policy to
0.869, and the difficulty router (B7) to 0.866. Two caveats: (i) the premium
model is also the routing reference and judge anchor, which can bias pairwise
results; (ii) this is a single deterministic pass. We therefore claim only that
\emph{quality remained comparable within the evaluated scope}
(Figure~\ref{fig:qvc}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig3_quality_vs_cost.png}
\caption{CACHED/JUDGE-ASSESSED: quality vs.\ cost in the open-ended harness
(sources: results/rq1\_cost.csv, results/rq2\_quality.csv). This is a proxy
quality signal, not objective correctness.}\label{fig:qvc}
\end{figure}

\subsection{RQ2b --- Objective accuracy + judge validity}
To remove judge bias and test generality, we evaluate on \textbf{500 real
public-benchmark items} --- 250 \textbf{GSM8K}~\cite{cobbe2021gsm8k} (math,
numeric) and 250 \textbf{MMLU}~\cite{hendrycks2021mmlu} (multiple-choice A--D)
--- scored by \textbf{objective exact-match}.
Premium-static (gpt-4o) reaches \textbf{65.4\%} accuracy at 0.116 EUR;
\textbf{Ours 47.2\%} at 0.0073 EUR (\textbf{$-94\%$ cost}); static-policy 59.4\%;
least-cost/difficulty-router 44--46\%. On these objective tasks aggressive cost
routing \emph{trades accuracy for cost}.

\paragraph{Why this contradicts the judge (a methodological finding).}
We re-scored 300 objective answers with \emph{both} exact-match and the LLM judge:
the judge rated \textbf{81.1\% of objectively \emph{incorrect} answers as
``acceptable'' ($\geq3$)}, with nearly identical mean judge scores for correct
(4.49) vs.\ incorrect (4.14) answers and near-zero point-biserial correlation
($r{=}0.12$). \textbf{The LLM-as-judge over-estimates quality on objective tasks}
--- it cannot distinguish fluent-but-wrong from correct
(Figure~\ref{fig:judge}). This explains the RQ2-vs-RQ2b gap, and we adopt the
consequence throughout: report judge-based quality only for open-ended workloads,
exact-match for tasks with ground truth, and never claim quality preservation
from the judge alone.

\begin{figure}[H]\centering
\includegraphics[width=0.72\linewidth]{fig_judge_vs_truth.png}
\caption{CACHED: LLM-judge score by objective correctness
(source: results/judgevstruth.csv); the judge over-rates wrong answers.}\label{fig:judge}
\end{figure}

\subsection{RQ3 --- Latency and overhead}
The \textbf{routing decision adds tens of microseconds} (B6 ${\approx}26.5\,\mu$s/
request) --- negligible vs.\ network latency. End-to-end latency tracks the chosen
model and provider-side variance. Notably, B6's p95 (3475 ms) exceeds
premium-static's (2494 ms), which we do not hide: B6 mixes models, giving a
heavier-tailed latency distribution. We resolve whether this is real or
single-pass noise in \S\ref{sec:stats}. Least-cost has the lowest latency
(p95 1476 ms), consistent with always picking small models
(Figure~\ref{fig:lat}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig4_latency.png}
\caption{CACHED/MEASURED LATENCY: latency percentiles by strategy in the harness
(source: results/rq3\_latency.csv); provider variance is not isolated.}\label{fig:lat}
\end{figure}

\paragraph{Gateway data-path overhead (MEASURED, standalone Envoy).}
Beyond control-plane decision time, we measured routing through a real
\textbf{Envoy} data path~\cite{envoygateway}: an Envoy proxy in front of the provider, driven at
concurrency 8. Envoy adds \textbf{negligible overhead} --- mean latency 1153 ms
vs.\ 1116 ms direct (${\approx}{+}3\%$, within run-to-run variance), throughput
6.05 vs.\ 5.81 req/s, \textbf{0 errors} --- supporting the feasibility of a
gateway-level control plane. The bounded kind verification above validates
in-cluster Envoy AI Gateway telemetry and attribution; production-grade
load/failover testing remains future work.

\subsection{RQ4 --- Sovereignty (\textbf{SIMULATED policy scenarios over real/catalogued providers})}
A sovereignty-blind baseline (B1) commits \textbf{40 violations} under eu-only,
france-only and self-hosted-only, and 10 under no-external-sensitive.
\textbf{B6-ours commits zero violations in every scenario.} Crucially, with a
\textbf{real EU provider} (Mistral-Large-3, Azure AI Foundry DataZone EU), under
the \textbf{EU-only} policy Ours \textbf{keeps 100\% availability (40/40 served)
with zero violations} by rerouting to Mistral EU (quality 0.866) --- where the
blind baseline would commit 40 violations. The \emph{cost of sovereignty} appears
in the stricter france-only/self-hosted-only scenarios: only the modeled FR
self-hosted model qualifies, so 20/40 are served and the rest refused rather than
violated --- quantifying the availability price of strict residency
(Figure~\ref{fig:sov}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig5_sovereignty.png}
\caption{SIMULATED POLICY SCENARIOS: sovereignty violations for a blind baseline
vs.\ Ours (source: results/rq4\_sovereignty.csv). Provider residency is declared
metadata, not legal certification.}\label{fig:sov}
\end{figure}

\subsection{RQ5 --- Budget-aware degradation (\textbf{SIMULATED budget pressure})}
Under a budget tight enough to be exceeded by always-premium,
\textbf{ours-graceful keeps 100\% availability with 0\% budget overrun}
(quality 0.85), whereas \textbf{hard-block} falls to \textbf{60\% availability}
(4/10 requests blocked) and alert-only overshoots the budget by 150\%. Graceful
degradation dominates on availability \emph{and} spend
(Figure~\ref{fig:budget}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig6_budget.png}
\caption{SIMULATED: budget policies, availability vs.\ overrun
(source: results/rq5\_budget.csv); no live provider is called in this scenario.}\label{fig:budget}
\end{figure}

\subsection{RQ6 --- Managed vs.\ self-hosted break-even (\emph{modeled})}
Using real per-token prices and modeled GPU+ops cost (2500 EUR/month, 5000 EUR
migration), managed cost grows with volume while self-hosted is flat. The
crossover is at \textbf{${\approx}25.6$M tokens/day} (payback ${\approx}4.3$
months); above it the engine recommends \emph{self-host}. This is a modeled
prediction to be validated against real GPU economics. It is decision support,
not a vLLM/GPU benchmark. Self-hosting can be worse below the break-even point,
under low utilization, with high reliability/operations cost, or when migration
cost dominates monthly savings
(Figure~\ref{fig:be}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig7_breakeven.png}
\caption{MODELED: managed vs.\ self-hosted cost vs.\ daily volume
(source: results/rq6\_breakeven.csv). This is not a GPU/vLLM measurement.}\label{fig:be}
\end{figure}

\subsection{Sensitivity analysis}
\label{sec:sensitivity}
We add local-only sensitivity tables generated by
\path{experimentation/scripts/sensitivity_analysis.py}. These analyses do not
call any provider. Scoring-weight and budget-threshold rows are
\textbf{SIMULATED}; break-even rows are \textbf{MODELED}.

\begin{table}[H]\centering\footnotesize
\caption{Sensitivity analysis. Source: experimentation/results/*sensitivity.csv.}
\label{tab:sensitivity}
\begin{tabular}{p{3.1cm}p{3.2cm}p{3.2cm}p{4.2cm}}
\toprule
Analysis & Low/aggressive case & Base case & Reviewer-facing interpretation \\
\midrule
Scoring weights & Cost/latency-heavy: 0.015533 EUR simulated cost, mean quality prior 0.8600 & Base: 0.016363 EUR, mean quality prior 0.9000 & Higher cost/latency pressure saves little in this catalog but chooses 20 cheapest-model routes; quality risk rises. \\
Budget threshold & At 40\% of premium spend, hard-block serves 6/10 and overruns 54.00\%; graceful serves 10/10 with 0\% overrun & At 100\%, all policies fit the budget & Graceful degradation is most valuable under tight budgets; hard-block can still overrun when a request crosses the threshold. \\
Break-even economics & Low-ops/high-utilization: break-even 14.9M tokens/day & Base analytical: 17.5M tokens/day & The plotted RQ6 curve samples the first positive point at 25.6M/day; exact break-even is lower because the curve is coarse. \\
Break-even downside & High-ops/low-utilization: break-even 87.7M tokens/day, keep-managed at 25M/day & API price -30\%: keep-managed at 25M/day & Self-hosting is not inherently cheaper; utilization and managed API price dominate. \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablation}
Removing the \textbf{cost term} reverts spend to premium (0.0389 EUR) at
unchanged quality --- the cost term produces the saving. Removing the
\textbf{quality term} drives spend to the floor (0.0012 EUR) but lowers quality
to 0.863 --- the quality term protects it. The latency term has negligible effect
here. The full system attains the cost saving \emph{and} the quality floor
(Figure~\ref{fig:abl}).

\begin{figure}[H]\centering
\includegraphics[width=0.82\linewidth]{fig8_ablation.png}
\caption{CACHED/SIMULATED: ablation of scoring terms in the harness
(source: results/rq8\_ablation.csv); it tests heuristic components, not
optimality.}\label{fig:abl}
\end{figure}

\subsection{Statistical significance (N=30 repetitions, \textbf{MEASURED fresh API calls})}
\label{sec:stats}
To move beyond the single deterministic pass, we re-ran the full matrix
\textbf{30 times} at temperature 0.7 with caches bypassed (independent samples;
judge $=$ gpt-4o-mini to bound cost), and tested each strategy against
premium-static on the per-repetition distributions (Mann-Whitney U, Cliff's
$\delta$, Cohen's $d$, 95\% CIs).
\begin{itemize}
  \item \textbf{Cost.} Ours 0.0110 EUR/rep [0.0109, 0.0110] vs.\ premium 0.0385
        [0.0382, 0.0389] --- a \textbf{71.4\% reduction},
        $p\approx3\times10^{-11}$, Cliff $\delta=-1.00$, Cohen $d=-43$
        (non-overlapping CIs): highly significant with a maximal effect size.
  \item \textbf{Quality.} Ours mean normalized quality 0.9587 [0.9564, 0.9611]
        vs.\ premium 0.9362 --- under this judge Ours is significantly higher
        ($p\approx6\times10^{-11}$, $\delta=+0.97$, $d=+3.4$), while na\"ive
        least-cost is significantly lower (0.869, $d=-13$). The \emph{sign} of the
        small quality gap depends on the judge (the single-pass gpt-4o judge
        showed parity), so we claim quality is \emph{at least comparable}.
  \item \textbf{Latency.} Ours is significantly higher (1506 ms/rep [1459, 1553]
        vs.\ 998, $p\approx5\times10^{-10}$, $d=+3.7$) --- confirming the
        routing-mix latency cost is \emph{real, not single-pass noise}. Least-cost
        is lowest (707 ms); the latency/cost trade-off is now quantified.
\end{itemize}

\begin{figure}[H]\centering
\includegraphics[width=0.49\linewidth]{fig_stats_cost.png}\hfill
\includegraphics[width=0.49\linewidth]{fig_stats_quality.png}
\\[4pt]
\includegraphics[width=0.49\linewidth]{fig_stats_latency.png}
\caption{MEASURED: per-repetition cost, quality and latency, mean $\pm$ 95\% CI
(N=30, source: results-stats/); the quality sign remains judge-dependent.}\label{fig:stats}
\end{figure}

% =====================================================================
\section{Discussion}
\label{sec:discussion}
Economic-aware routing is most valuable when a workload mixes easy and hard
requests: the router sends cheap requests to cheap models and reserves the premium
model for quality-critical ones, capturing most of the savings at little quality
cost. Strict sovereignty has a real availability/quality price that the system
makes explicit and auditable rather than hidden. For budget control, graceful
degradation strictly dominates hard-blocking on availability and overrun.
Self-hosting pays off only at high, sustained volume.

\subsection{Reviewer-facing questions}
\paragraph{Why not just use LiteLLM?} LiteLLM is a practical gateway with spend
tracking, keys/teams, budgets, retries, and fallbacks. Our operator adds a
Kubernetes reconciliation layer around gateway telemetry: CRD-declared policies,
namespace/application attribution, hard sovereignty filtering before scoring,
decision traces, and break-even decision support. The systems are complementary.

\paragraph{Why not just use RouteLLM or another learned router?} Learned routers
estimate request difficulty and can improve the cost--quality frontier. They do
not decide whether a candidate violates residency, a tenant budget, or an audit
policy. In our architecture a learned router can rank candidates only after the
hard governance filter has removed non-compliant options.

\paragraph{What does Kubernetes add?} Kubernetes provides the ownership boundary
that enterprises already operate: namespaces, deployments, service accounts,
labels, CRDs, events, and reconciliation. This makes spend and policy decisions
attributable to the workload rather than only to an API key.

\paragraph{What is actually measured?} The paper measures cached and fresh LLM
calls in the harness, a 30-repetition uncached statistical run, standalone Envoy
data-path overhead, and a bounded live kind run proving in-cluster gateway
telemetry plus Tetragon shadow-AI ingestion. It does not measure production
failover, multi-cluster scale, or real GPU serving.

\paragraph{What is modeled?} Self-hosting economics and the self-hosted fallback
quality/cost are modeled. Sensitivity analysis varies GPU+ops cost, utilization,
migration cost, and managed API price; it is decision support only.

Table~\ref{tab:recap} consolidates every research question into a single
gains/losses ledger versus the always-premium baseline, so the trade-off is
visible at a glance: large, statistically significant cost and governance gains,
bought with a real latency increase and --- on objective tasks --- a real
accuracy loss.

\begin{table}[H]
\centering\footnotesize
\caption{Recap of gains and losses vs.\ the always-premium baseline.
\cmark~=~net gain, \xmark~=~net loss, $\approx$~=~parity/neutral, $+$~=~new
capability the baseline lacks.}
\label{tab:recap}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{>{\raggedright\arraybackslash}p{3.2cm}
                >{\raggedright\arraybackslash}p{2.3cm}
                >{\raggedright\arraybackslash}p{2.3cm}
                >{\raggedright\arraybackslash}p{3.0cm} c}
\toprule
Dimension & Premium baseline & Ours & Net effect & \\
\midrule
Total cost (40-prompt matrix, cached harness) & 0.0389 EUR & 0.0114 EUR & $-70.8\%$ & \cmark \\
Cost (N=30, significance)     & 0.0385 EUR & 0.0110 EUR & $-71.4\%$, $p{\approx}3{\times}10^{-11}$, $d{=}{-}43$ & \cmark \\
Quality --- open-ended (judge) & 0.900 & 0.9125 & comparable / parity (win-rate 50\%) & $\approx$ \\
Quality --- objective (exact-match, 500 items) & 65.4\% & 47.2\% & $-18.2$ pts (but $-94\%$ cost) & \xmark \\
Latency (mean, N=30) & 998 ms & 1506 ms & $+51\%$, $d{=}{+}3.7$ & \xmark \\
Routing decision overhead & --- & ${\approx}26.5\,\mu$s & negligible & $\approx$ \\
Sovereignty violations (simulated policy scenarios) & 40 & 0 & zero violations under declared metadata & \cmark \\
EU-only availability (simulated policy scenario) & 0\% (blind) & 100\% & served via declared EU provider & \cmark \\
Budget availability / overrun (simulated pressure) & 60\% / overrun & 100\% / 0\% & graceful degradation & \cmark \\
Live kind verification & none & 4 apps + shadow-AI & real gateway telemetry and Tetragon evidence & + \\
Cost attribution, budgets, break-even & none & present & governance layer & $+$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{When \emph{not} to use economic routing}
Economic routing is not a universal win; our own results delimit where to avoid it:
\begin{itemize}
  \item \textbf{High-stakes, correctness-critical tasks.} On objective reasoning
        (RQ2b) cheaper models genuinely fail, so cost routing trades accuracy for
        cost (47.2\% vs.\ 65.4\%). Where a wrong answer is expensive (legal,
        medical, shipping code, financial calculations), pin the premium model.
  \item \textbf{Low volume / low spend.} Below a few hundred thousand tokens/month
        the absolute saving is small and does not justify the added control-plane
        complexity; premium-static is simpler.
  \item \textbf{Strict tail-latency SLOs.} Ours shows a significant tail-latency
        increase (RQ3, $d{=}+3.7$) from mixing models; for ultra-low-latency
        paths, a single fast model is preferable.
  \item \textbf{No reliable quality signal.} Our priors miss task-specific
        difficulty and the LLM judge over-rates wrong answers (RQ2b); without a
        trustworthy per-task quality estimate, routing can silently degrade
        quality --- prefer a fixed model or add a calibrated/learned difficulty
        predictor first.
  \item \textbf{No governance need.} If there is no sovereignty, budget, or
        attribution requirement (single tenant, single jurisdiction, flat
        budget), the governance layer adds little; a plain cost--quality router
        (or none) suffices.
  \item \textbf{Homogeneous high-quality workloads.} If essentially every request
        needs the premium model, there is nothing to route --- the policy
        degenerates to premium-static.
\end{itemize}
In short, our approach targets \textbf{multi-tenant, governance-constrained,
mixed-difficulty, sufficiently high-volume} settings; outside that envelope its
benefits shrink or invert.

% =====================================================================
\section{Planned Study: Human FinOps Expert vs.\ Operator (RQ7)}
\label{sec:rq7}
The evaluation so far compares the operator against \emph{algorithmic} baselines.
A complementary question --- reserved for a future version of this work and stated
here for transparency --- is how close the control plane's decisions are to those
of \emph{experienced human practitioners}. We have \textbf{not} run this study and
report no results; we publish the protocol so the claim space is explicit and the
study is pre-registered.

\paragraph{Research question.} How closely do the control plane's routing and
managed-vs-self-host recommendations match the decisions of experienced
FinOps/cloud experts, and where do they diverge?

\paragraph{Design.} $N\geq6$--$10$ practitioners (cloud/FinOps/platform engineers
with LLM-ops experience) each work through a set of decision scenarios drawn from
the testbed. Every scenario provides a workload profile, the model/provider
catalog with prices, the sovereignty policy, the budget state, and recent usage
telemetry. For each scenario the expert chooses (a) which model/provider to route
to and (b) the managed-vs-self-host recommendation, with a short written
rationale. The operator is run on the \emph{same} scenarios to produce the same
two decisions.

\paragraph{Metrics.} (i) \textbf{Agreement} between expert and system: exact-match
rate on the model/provider choice and on the host/keep-managed recommendation,
summarized with Cohen's $\kappa$ across experts and against the system; (ii)
\textbf{decision time}, human vs.\ automated; (iii) \textbf{rationale overlap} ---
which signals experts cite (cost, sovereignty, budget, quality) vs.\ the system's
stated reasons; (iv) a qualitative \textbf{disagreement analysis} of where and why
the two diverge.

\paragraph{Threats and honesty.} The expert pool is small, scenario realism is
imperfect, and experts may lack full price visibility; we mitigate with diverse
recruitment, realistic telemetry, and pre-registration of the analysis. Until the
study is run, no agreement scores or expert decisions are reported --- RQ7 remains
explicitly future work (protocol: \texttt{RQ7\_HUMAN\_EXPERT\_PROTOCOL.md}).

% =====================================================================
\section{Threats to Validity}
\label{sec:threats}
\textbf{Internal validity.} Provider APIs, latency, quotas, and prices vary over
time. We mitigate deterministic runs with committed caches and report a
30-repetition fresh run for distributional evidence, but future API changes can
alter absolute costs or latency.

\textbf{Construct validity.} LLM-as-judge acceptability is only a proxy for
open-ended quality. RQ2b shows the judge over-rates objectively wrong answers, so
we separate judge-assessed quality from exact-match accuracy and avoid universal
quality-preservation claims.

\textbf{External validity.} The harness uses two real managed providers, four
synthetic enterprise workloads, and no production traces. The Mistral deployment
uses Azure AI Foundry DataZoneStandard (EU data zone); strict France-only
residency still relies on modeled/self-hosted options.

\textbf{System validity.} The bounded kind run proves telemetry scrape,
namespace/application attribution, cost status, and Tetragon shadow-AI ingestion
under a small local workload. It does not prove production-grade Envoy AI Gateway
enforcement, failover, high concurrency, or multi-cluster behaviour.

\textbf{Economic validity.} RQ6 is modeled. It is not a GPU/vLLM measurement and
does not include batching efficiency, model-size effects, cache-hit rates,
reliability targets, reserved-capacity discounts, or engineering on-call cost
beyond the configured scenarios.

\textbf{Regulatory validity.} Sovereignty decisions rely on declared metadata.
The operator can enforce technical routing policies and preserve audit evidence,
but it cannot independently certify provider legal compliance or contractual data
residency.

\textbf{Adversarial validity.} Teams could mislabel sensitivity, provider
metadata could be stale, or direct API calls could bypass the gateway. Shadow-AI
detection reduces this blind spot but does not replace network policy, identity
controls, procurement governance, and LLM-specific security controls such as
those catalogued by OWASP~\cite{owaspllm2025}.

\textbf{Scalability validity.} Many namespaces, many providers, and policy
explosion are not stress-tested here. Larger deployments need load testing of the
reconciliation loop, CRD cardinality, metrics cardinality, and policy conflict
resolution.

% =====================================================================
\section{Conclusion and Future Work}
\label{sec:conclusion}
Within our preliminary, two-provider testbed, a gateway-level, economic-aware,
sovereignty-constrained, budget-aware control plane substantially reduced cached
harness cost ($-70.8\%$) while keeping open-ended judge quality comparable, and
the simulated policy scenarios showed zero violations under declared provider
metadata and better availability under budget pressure. A bounded live kind run
then verified the engineering path from Envoy AI Gateway token telemetry and
Tetragon shadow-AI events into operator status and metrics, with automatic
cleanup. We deliberately frame these as \emph{preliminary evidence and a
reproducible framework}, not universal or production-grade claims. To reach a
defensible Q1 result we plan: (1) more providers; (2) real GPU self-hosting to
validate the modeled break-even; (3) multi-judge + human quality evaluation; (4)
human evaluation to settle the judge-dependent quality sign (the 100-item package
is ready; the N=30 statistics and the judge-vs-ground-truth study are done); (5)
production-grade Envoy AI Gateway enforcement with load testing and failover; (6)
a human FinOps expert vs.\ automated control-plane study; and (7) integrating a
learned difficulty router with our governance constraints. The paper claims only
what the committed results support.

\paragraph{Artifact / Reproducibility.} The operator and \path{experimentation/}
(datasets, harness, scripts, results, figures, cached responses, and live kind
evidence bundles) constitute the reproducible artifact. Reproduce harness results
with \path{scripts/run_experiment.sh} then
\path{python3 scripts/analyze_results.py}; reproduce the bounded live
verification with \path{make -C automatisation real-demo-verify}; regenerate the
local sensitivity tables with \path{python3 scripts/sensitivity_analysis.py}.
Every reported test is journaled; failed or negative runs are preserved rather
than relabeled.

% =====================================================================
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
