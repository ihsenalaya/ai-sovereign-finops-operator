# Tables

Canonical tables are now embedded in `latex/main.tex`:

- Evidence map.
- Workload construction.
- Objective benchmark and guardrail recomposition.
- Implementation/evaluation status.
- Capability positioning.

The numeric sources are the committed CSV files under `../results`,
`../results-stats`, and `../results-bench`.
